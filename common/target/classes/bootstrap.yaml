spring:
  cloud:
    config:
      label: master
      name: common
      discovery:
        enabled: true
        service-id: config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8671/eureka/

#监控
management:
  endpoints:
    web:
      exposure:
        include: refresh